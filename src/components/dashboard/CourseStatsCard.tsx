"use client";

import React from 'react';

interface CourseStatsCardProps {
  purchasedCoursesCount: number;
}

// This card is no longer needed, so we return null
const CourseStatsCard: React.FC<CourseStatsCardProps> = () => {
  return null;
};

export default CourseStatsCard;