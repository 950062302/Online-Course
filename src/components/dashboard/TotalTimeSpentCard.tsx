"use client";

import React from 'react';

interface TotalTimeSpentCardProps {
  totalTimeSpentSeconds: number;
}

// This card is no longer needed, so we return null
const TotalTimeSpentCard: React.FC<TotalTimeSpentCardProps> = () => {
  return null;
};

export default TotalTimeSpentCard;